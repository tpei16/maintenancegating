# ---- data (FMUCD is openly available from Mendeley Data; not redistributed) ----
data/

# ---- drafting and working documents kept outside the published artefact ----
*revision_plan*.md
*Revision_Plan*.md
*review_plan*.md
*Review_Plan*.md
*submission_plan*.md
*Submission_Plan*.md
*Revision_Plan*.pdf

# ---- internal working material: not part of the published artefact ----
notes/
paper/
logs/
checkpoints/
manuscript_rev/

# ---- the manuscript itself is NOT part of the reproduction artefact.
#      It carries author names, e-mails and affiliation, which would break the
#      anonymity of the review copy served via anonymous.4open.science. ----
manuscript/


# ---- LaTeX build intermediates ----
*.aux
*.abs
*.bbl
*.blg
*.log
*.out
*.spl
*.synctex.gz
*.fdb_latexmk
*.fls
*.toc

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/

# ---- environment / editor / OS ----
.env
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---- regenerable intermediate caches (56 MB): the event stream rebuilt by
#      scripts/50 and the out-of-fold base scores cached by scripts/52. Both are
#      derived from the open dataset by the scripts in this repository, so they
#      are reproduced rather than redistributed. ----
results/wp1/event_sequences.parquet
results/wp3/base_scores_*.parquet

# ---- leave-one-out and leave-two-out held-out predictions. Regenerated by
#      scripts/44 and scripts/48 from the open dataset in under three minutes,
#      so they are reproduced rather than redistributed. ----
data/processed/pred_pair/
data/processed/pred_single/
data/processed/pred_pair_logreg/
data/processed/pred_single_logreg/
